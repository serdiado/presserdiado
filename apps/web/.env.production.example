# =============================================================================
# MatbaaPro Web — Production (.env.production.example)
#
# Vite build sirasinda `--mode production` ile bu dosya yuklenir.
# `apps/web/.env.production` olarak kopyalayin (gitignore'da).
#
# Domain plani:
#   Web : https://serdiado.de       (bu uygulama)
#   API : https://api.serdiado.de   (Fastify backend)
# =============================================================================

# API root — sonunda /api/v1 olacak sekilde
VITE_API_URL=https://api.serdiado.de/api/v1
